package components.musicPlaylist;

/**
 * Type {@code Song} used for storing and retreiving name and duration.
 *
 * @param name
 * @param duration
 */
public record Song(String name, int duration) {
}
